{
  "buildCommand": "NODE_OPTIONS=--max-old-space-size=6144 bun run build",
  "outputDirectory": "dist/client",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/api/$1"
    }
  ]
}
